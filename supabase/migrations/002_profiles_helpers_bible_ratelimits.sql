-- Migration 002: profiles, helper functions, bible_verses, rate_limits
-- Ver apply_migration via MCP para o SQL completo aplicado.
-- Este arquivo é o espelho local para versionamento.

-- profiles, is_admin(), is_approved(), immutable_unaccent()
-- bible_verses (FTS via tsvector generated column)
-- rate_limits
-- Ver histórico de migrations no Supabase Dashboard > Database > Migrations
